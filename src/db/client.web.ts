// Web preview stub — expo-sqlite's web backend requires SharedArrayBuffer
// (cross-origin isolation headers) that the dev server doesn't provide.
// Native platforms use client.ts (real SQLite) via Metro's .web extension resolution.
export const db = null;

export async function initDatabase() {}
